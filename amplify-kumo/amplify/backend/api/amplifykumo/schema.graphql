type Todo @model {
  id: ID!
  name: String!
  description: String
  author: String
}

type Account @model {
  id: ID!
  accountid: String!
  name: String!
  email: String!
  num: Int!
  status: String!


}

type Comments @model {
  id: ID!
  author: String!
  description: String
  priority: Int
}
